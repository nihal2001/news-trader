version: '3.11'
services:
  scraper:
    build:
      context: ./yahoo
    env_file:
      - ./yahoo/.env
    volumes:
      - shared_volume:/shared

  uploader:
    build:
      context: ./uploader
    env_file:
      - ./uploader/.env
    volumes:
      - shared_volume:/shared
    depends_on:
      - scraper

volumes:
  shared_volume:


